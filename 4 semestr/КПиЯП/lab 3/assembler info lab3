Лаба 3

Арифмитический - логические операции

сдвиги (вл | впр) (ар\лог)
	

операнды
	1 в переменных [косв.]
	2 в регистнах  [прямая регистр.\ковс.]
	3 в команде    [непосредственная]

call
	1 в стэке сохраняется адрес после call
		1.1 для tiny+compact сохранятеся значение ip
		1.2 для medium+large сохраняется cs + ip
	2 в регистры cs:ip для больших моделей или ip для малых
	  загружается адрес точки входа подпрограммы
	3 след исполняемая программа будет первая команда подпрограммы
	4 последняя команда должны быть ret

	
как передать параметры
	1 стэк
	2 регистры
	
прерывание == функция которая находтся в far сегменте

стэк растет вниз!

прерывания
	(абстракция) таблица векторов прерываний (первый кб памяти)
	вектор = 4байта
	что делает int N
		1 сохраняем cs, ip, flag
		2 jmpfar [(сегм.арес)N*4+2]:[(эфф.адрес)N*4] 
		3 iret (pop flag pop ip pop cs)
	
Если есть арифмитические операции - то надо добавить логические
Если не сказано что нельзя - то можно (главный факт)
если надо округлять - нужно объяснить как
Ввод отрицательных чисел требуется

FAQ
	перед тем как работать с числами нужно из символа отнять 30h (Проблема ввода чисел)
	переполнение при арифм операциях (150+150 не поместится в байт :D)
	процедуры на ассемблере
	
Отчет
	1 титульный лист
	2 задание
	3 скрины выполнения задания с маленькими комметариями
	4 листинг программы
